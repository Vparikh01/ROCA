# ACO + NLP + RL

Multi-phase Ant Colony Optimization engine with:
- Core ACO engine + Visualizer
- NLP Embedding Layer for constraint-aware routing
- Policy-based RL integration

## Installation
```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt